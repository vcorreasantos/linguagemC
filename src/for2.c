#include <stdio.h>
#include <stdlib.h>

int main(){ //fazer a soma em decresente e quase a mesma coisa do
    system("clear");
    int i;
    for(i = 10 ; i >= 1 ; i--){
        printf("%d\n",i);
    }
    return 0;
}